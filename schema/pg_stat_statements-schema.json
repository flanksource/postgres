{
  "$id": "https://github.com/flanksource/postgres/schema/pg_stat_statements-schema.json",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "description": "pg_stat_statements extension configuration parameters",
  "properties": {
    "pg_stat_statements.max": {
      "type": "integer",
      "description": "Maximum number of statements tracked by pg_stat_statements",
      "default": 5000,
      "minimum": 100,
      "maximum": 2147483647
    },
    "pg_stat_statements.track": {
      "type": "string",
      "description": "Which statements are tracked by pg_stat_statements",
      "default": "top",
      "enum": ["none", "top", "all"]
    },
    "pg_stat_statements.track_utility": {
      "type": "boolean",
      "description": "Whether utility commands are tracked by pg_stat_statements",
      "default": true
    },
    "pg_stat_statements.track_planning": {
      "type": "boolean",
      "description": "Whether planning statistics are tracked by pg_stat_statements",
      "default": false
    },
    "pg_stat_statements.save": {
      "type": "boolean",
      "description": "Save pg_stat_statements statistics across server shutdowns",
      "default": true
    }
  }
}