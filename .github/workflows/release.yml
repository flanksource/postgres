name: Release PostgreSQL Upgrade Images

on:
  push:
    branches:
      - main
      - master
    tags: ["v*"]
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag"
        required: true
        default: "v1.0.0"

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/postgres

jobs:
  determine-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      version: ${{ steps.version.outputs.version }}
      is_new_version: ${{ steps.version.outputs.is_new_version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install svu
        run: |
          wget -qO /tmp/svu.tar.gz https://github.com/caarlos0/svu/releases/download/v3.2.4/svu_3.2.4_linux_amd64.tar.gz
          tar -xzf /tmp/svu.tar.gz -C /tmp
          sudo mv /tmp/svu /usr/local/bin/
          chmod +x /usr/local/bin/svu

      - name: Determine next version
        id: version
        run: |
          # If triggered by a tag, use that tag
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            VERSION="${{ github.ref_name }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "is_new_version=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # If workflow_dispatch with tag input, use that
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]] && [[ -n "${{ github.event.inputs.tag }}" ]]; then
            VERSION="${{ github.event.inputs.tag }}"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "is_new_version=false" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Otherwise, determine next patch version using svu
          CURRENT_VERSION=$(svu current || echo "v0.0.0")
          NEXT_VERSION=$(svu patch)

          echo "Current version: $CURRENT_VERSION"
          echo "Next version: $NEXT_VERSION"

          echo "version=$NEXT_VERSION" >> $GITHUB_OUTPUT
          echo "is_new_version=true" >> $GITHUB_OUTPUT

      - name: Create and push version tag
        if: steps.version.outputs.is_new_version == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git tag -a "${{ steps.version.outputs.version }}" -m "Release ${{ steps.version.outputs.version }}"
          git push origin "${{ steps.version.outputs.version }}"

  build-images-for-version-detection:
    needs: determine-version
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pg_version: [16, 17]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build image for version detection
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64
          build-args: |
            PG_VERSION=${{ matrix.pg_version }}
          push: false
          tags: postgres:${{ matrix.pg_version }}
          load: true
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Save image as artifact
        run: |
          docker save postgres:${{ matrix.pg_version }} > postgres-${{ matrix.pg_version }}.tar

      - name: Upload image artifact
        uses: actions/upload-artifact@v4
        with:
          name: postgres-${{ matrix.pg_version }}
          path: postgres-${{ matrix.pg_version }}.tar
          retention-days: 1

  get-postgres-versions:
    needs: build-images-for-version-detection
    runs-on: ubuntu-latest
    outputs:
      pg16_version: ${{ steps.versions.outputs.pg16_version }}
      pg17_version: ${{ steps.versions.outputs.pg17_version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download PostgreSQL 16 image
        uses: actions/download-artifact@v6
        with:
          name: postgres-16

      - name: Download PostgreSQL 17 image
        uses: actions/download-artifact@v6
        with:
          name: postgres-17

      - name: Load Docker images
        run: |
          docker load < postgres-16.tar
          docker load < postgres-17.tar

      - name: Get PostgreSQL versions from built images
        id: versions
        run: |
          chmod +x scripts/get-postgres-versions.sh
          ./scripts/get-postgres-versions.sh

  build-postgres-cli:
    needs: determine-version
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    strategy:
      matrix:
        include:
          - goos: linux
            goarch: amd64
            binary: postgres-cli-linux-amd64
          - goos: linux
            goarch: arm64
            binary: postgres-cli-linux-arm64
          - goos: darwin
            goarch: amd64
            binary: postgres-cli-darwin-amd64
          - goos: darwin
            goarch: arm64
            binary: postgres-cli-darwin-arm64
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - name: Build postgres-cli (${{ matrix.goos }}-${{ matrix.goarch }})
        env:
          VERSION: ${{ needs.determine-version.outputs.version }}
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
        run: |
          # Remove 'v' prefix if present
          VERSION_NO_V="${VERSION#v}"

          # Build for specific architecture
          go build -ldflags "-s -w -X main.version=${VERSION_NO_V}" -o ${{ matrix.binary }} ./cmd

      - name: Upload binary (${{ matrix.binary }})
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary }}
          path: ${{ matrix.binary }}
          retention-days: 7

  build-per-arch:
    needs: [determine-version, build-images-for-version-detection, get-postgres-versions]
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        include:
          # PostgreSQL 16 builds
          - runner: ubuntu-latest
            arch: amd64
            platform: linux/amd64
            pg_version: 16
          - runner: ubuntu-24.04-arm
            arch: arm64
            platform: linux/arm64
            pg_version: 16
          # PostgreSQL 17 builds
          - runner: ubuntu-latest
            arch: amd64
            platform: linux/amd64
            pg_version: 17
          - runner: ubuntu-24.04-arm
            arch: arm64
            platform: linux/arm64
            pg_version: 17
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set version variables
        id: versions
        run: |
          if [ "${{ matrix.pg_version }}" = "16" ]; then
            echo "minor_version=${{ needs.get-postgres-versions.outputs.pg16_version }}" >> $GITHUB_OUTPUT
          else
            echo "minor_version=${{ needs.get-postgres-versions.outputs.pg17_version }}" >> $GITHUB_OUTPUT
          fi

          # Extract git tag (remove 'v' prefix if present)
          TAG="${{ github.ref_name }}"
          if [[ "$TAG" == v* ]]; then
            TAG="${TAG#v}"
          fi
          echo "release_tag=$TAG" >> $GITHUB_OUTPUT

          # Generate SHA short
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Build and push PostgreSQL ${{ matrix.pg_version }} (${{ matrix.arch }})
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: ${{ matrix.platform }}
          build-args: |
            TARGET_VERSION=${{ matrix.pg_version }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ matrix.pg_version }}-${{ matrix.arch }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.versions.outputs.minor_version }}-${{ steps.versions.outputs.sha_short }}-${{ matrix.arch }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.versions.outputs.minor_version }}-${{ matrix.arch }}
          cache-from: type=gha,scope=${{ matrix.pg_version }}-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.pg_version }}-${{ matrix.arch }}

  merge-manifests:
    needs: [build-per-arch, get-postgres-versions]
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    strategy:
      matrix:
        pg_version: [16, 17]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set version variables
        id: versions
        run: |
          if [ "${{ matrix.pg_version }}" = "16" ]; then
            echo "minor_version=${{ needs.get-postgres-versions.outputs.pg16_version }}" >> $GITHUB_OUTPUT
          else
            echo "minor_version=${{ needs.get-postgres-versions.outputs.pg17_version }}" >> $GITHUB_OUTPUT
          fi

          # Extract git tag (remove 'v' prefix if present)
          TAG="${{ github.ref_name }}"
          if [[ "$TAG" == v* ]]; then
            TAG="${TAG#v}"
          fi
          echo "release_tag=$TAG" >> $GITHUB_OUTPUT

          # Generate SHA short
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create and push multi-arch manifest for PostgreSQL ${{ matrix.pg_version }}
        run: |
          # Create multi-arch manifest for major version tag
          docker buildx imagetools create -t \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ matrix.pg_version }}

          # Create multi-arch manifest for minor version tag
          docker buildx imagetools create -t \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.versions.outputs.minor_version }}
          # Create multi-arch manifest for version with SHA
          docker buildx imagetools create -t \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.versions.outputs.minor_version }}-${{ steps.versions.outputs.sha_short }}

      - name: Create and push latest tag for PostgreSQL 17
        if: matrix.pg_version == '17'
        run: |
          docker buildx imagetools create -t \
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest
  create-release:
    needs: [determine-version, build-postgres-cli, merge-manifests, get-postgres-versions]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download all postgres-cli binaries
        uses: actions/download-artifact@v6
        with:
          pattern: postgres-cli-*
          path: ./binaries
          merge-multiple: true

      - name: Set version variables
        id: versions
        run: |
          # Use version from determine-version job
          VERSION="${{ needs.determine-version.outputs.version }}"
          # Remove 'v' prefix if present
          TAG="${VERSION#v}"
          echo "release_tag=$TAG" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          # Generate SHA short
          echo "sha_short=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

      - name: Create release notes
        id: release-notes
        run: |
          cat > release-notes.md << EOF
          # PostgreSQL Upgrade Images Release ${{ steps.versions.outputs.release_tag }}

          ## Available Images

          ### PostgreSQL 16 (Current: ${{ needs.get-postgres-versions.outputs.pg16_version }})
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:16\` - Latest PostgreSQL 16 major version
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.get-postgres-versions.outputs.pg16_version }}\` - Specific minor version
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.get-postgres-versions.outputs.pg16_version }}-${{ steps.versions.outputs.sha_short }}\` - Version with commit SHA

          ### PostgreSQL 17 (Current: ${{ needs.get-postgres-versions.outputs.pg17_version }})
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:17\` - Latest PostgreSQL 17 major version
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.get-postgres-versions.outputs.pg17_version }}\` - Specific minor version
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ needs.get-postgres-versions.outputs.pg17_version }}-${{ steps.versions.outputs.sha_short }}\` - Version with commit SHA
          - \`${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest\` - Points to PostgreSQL 17

          ## Usage

          ### Basic Upgrade
          \`\`\`bash
          docker run --rm \\
            -v /path/to/pgdata:/var/lib/postgresql/data \\
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:17
          \`\`\`

          ### Upgrade and Start PostgreSQL
          \`\`\`bash
          docker run -d \\
            -v /path/to/pgdata:/var/lib/postgresql/data \\
            -e START_POSTGRES=true \\
            -p 5432:5432 \\
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:17
          \`\`\`

          ## Supported Upgrade Paths

          - PostgreSQL 14 → 15, 16, 17
          - PostgreSQL 15 → 16, 17
          - PostgreSQL 16 → 17

          Sequential upgrades are performed automatically (e.g., 14→15→16→17).

          ## Environment Variables

          - \`PG_VERSION\`: Target PostgreSQL version (16 or 17)
          - \`START_POSTGRES\`: Start PostgreSQL after upgrade (default: false)
          - \`AUTO_UPGRADE\`: Enable auto-upgrade (default: true)
          - \`RESET_PASSWORD\`: Reset password on startup (default: false)
          - \`POSTGRES_PASSWORD\`: Password for reset (required if RESET_PASSWORD=true)

          ## Architecture Support

          All images support both \`linux/amd64\` and \`linux/arm64\` architectures (built natively on respective runners).

          ## Build Information

          - **AMD64 builds**: Native build on \`ubuntu-latest\`
          - **ARM64 builds**: Native build on \`ubuntu-24.04-arm\`
          - **Manifests**: Merged using Docker Buildx imagetools

          ## postgres-cli

          Download the CLI binary for your platform:
          - [Linux AMD64](https://github.com/${{ github.repository }}/releases/download/${{ steps.versions.outputs.version }}/postgres-cli-linux-amd64)
          - [Linux ARM64](https://github.com/${{ github.repository }}/releases/download/${{ steps.versions.outputs.version }}/postgres-cli-linux-arm64)
          - [macOS AMD64](https://github.com/${{ github.repository }}/releases/download/${{ steps.versions.outputs.version }}/postgres-cli-darwin-amd64)
          - [macOS ARM64 (Apple Silicon)](https://github.com/${{ github.repository }}/releases/download/${{ steps.versions.outputs.version }}/postgres-cli-darwin-arm64)

          Checksums are automatically generated by GitHub for all release assets.
          EOF

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.versions.outputs.version }}
          name: PostgreSQL Upgrade Images ${{ steps.versions.outputs.release_tag }}
          body_path: release-notes.md
          draft: false
          prerelease: false
          files: ./binaries/postgres-cli-*
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  create-git-tags:
    needs: [determine-version, get-postgres-versions, merge-manifests]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Create version-specific git tags
        run: |
          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Create tags for each PostgreSQL version
          SHA_SHORT=$(git rev-parse --short HEAD)

          # PostgreSQL 16 tags
          PG16_TAG="pg16-${{ needs.get-postgres-versions.outputs.pg16_version }}-${SHA_SHORT}"
          git tag -a "$PG16_TAG" -m "PostgreSQL 16.${{ needs.get-postgres-versions.outputs.pg16_version }} release"

          # PostgreSQL 17 tags
          PG17_TAG="pg17-${{ needs.get-postgres-versions.outputs.pg17_version }}-${SHA_SHORT}"
          git tag -a "$PG17_TAG" -m "PostgreSQL 17.${{ needs.get-postgres-versions.outputs.pg17_version }} release"

          # Push tags
          git push origin "$PG16_TAG"
          git push origin "$PG17_TAG"

          echo "Created tags: $PG16_TAG, $PG17_TAG"

  publish-helm-chart:
    needs: [determine-version, merge-manifests]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout postgres repository
        uses: actions/checkout@v4
        with:
          path: postgres

      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: "latest"

      - name: Set version from determine-version job
        id: version
        run: |
          # Use version from determine-version job
          VERSION="${{ needs.determine-version.outputs.version }}"
          # Remove 'v' prefix if present
          if [[ "$VERSION" == v* ]]; then
            VERSION="${VERSION#v}"
          fi
          echo "chart_version=$VERSION" >> $GITHUB_OUTPUT

      - name: Update Chart.yaml with release version
        run: |
          cd postgres/chart
          sed -i "s/^version:.*/version: ${{ steps.version.outputs.chart_version }}/" Chart.yaml
          sed -i "s/^appVersion:.*/appVersion: \"17\"/" Chart.yaml

      - name: Package Helm chart
        run: |
          cd postgres
          helm package chart/
          echo "Packaged chart: $(ls -1 *.tgz)"

      - name: Checkout flanksource/charts repository
        uses: actions/checkout@v4
        with:
          repository: flanksource/charts
          path: charts
          token: ${{ secrets.FLANKBOT }}

      - name: Copy chart to charts repository
        run: |
          mkdir -p charts/docs
          cp postgres/postgres-*.tgz charts/docs/

      - name: Update Helm repository index
        run: |
          cd charts/docs
          helm repo index . --url https://flanksource.github.io/charts

      - name: Commit and push to charts repository
        run: |
          cd charts
          git config user.name "flankbot"
          git config user.email "flankbot@users.noreply.github.com"
          git add docs/
          git commit -m "Add postgres chart version ${{ steps.version.outputs.chart_version }}"
          git push
