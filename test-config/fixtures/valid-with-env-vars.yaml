# Configuration with environment variable references
postgres:
  max_connections: ${PG_MAX_CONNECTIONS:100}
  shared_buffers: ${PG_SHARED_BUFFERS:128MB}
  
pgbouncer:
  listen_port: ${PGBOUNCER_PORT:6432}
  
postgrest:
  db_uri: ${DATABASE_URL}
  
walg:
  s3_prefix: ${WAL_S3_PREFIX}